function getMiddle(s)
{
var wordLength = s.length;
var middleChars;
if(wordLength % 2 == 0){
middleChars = s.substr((wordLength / 2 - 1),2); 
}
else{ 
middleChars = s.substr((wordLength / 2),1);
}
return middleChars;
}
