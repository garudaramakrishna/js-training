function rentalCarCost(noOfDays)
{
var discount; 
var rentForOneDay = 40;
  if(noOfDays >= 7){
  discount = 50;
  }
  else if (noOfDays >= 3){
  discount = 20; }
  else{
  discount = 0;
  }
  return (noOfDays * rentForOneDay) - discount;
  }
